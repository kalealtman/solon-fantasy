# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Secrets / credentials
.env
oauth2.json
.browser_profile/

# Cache
.cache/

# Data — raw pulls are regenerable and gitignored, processed CSVs are committed
data/raw/

# Site build output — regenerated from site/template.html + site/app.js +
# data/processed/*.csv via scripts/build_site.py; source is committed, the
# combined/baked file isn't
site/dist/

# OS
.DS_Store
Thumbs.db

# Coaches Poll raw form export -- the form promises anonymous responses not
# tied to results, so raw per-voter ballots never get committed. Only the
# aggregated data/processed/coaches_poll_*.csv (built by
# scripts/build_coaches_poll.py) is checked in.
*Coaches Poll*.xlsx
